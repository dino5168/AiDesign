import { PageShell } from "@/components/page-shell"

/**
 * Blog listing page (placeholder).
 *
 * Returns:
 *     The Blog page section.
 */
export function Blog() {
  return (
    <PageShell
      title="Blog"
      description="設計與工程筆記。文章列表建置中。"
    />
  )
}
