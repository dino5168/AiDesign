import { PageShell } from "@/components/page-shell"

/**
 * Works / portfolio listing page (placeholder).
 *
 * Returns:
 *     The Works page section.
 */
export function Works() {
  return (
    <PageShell
      title="Works"
      description="精選專案與作品集。內容建置中,之後將以卡片網格呈現各專案。"
    />
  )
}
